
# 构建配置中心
spring:
  application:
    name: category-service
  cloud:
    config:
      discovery:
        serviceId: config-server
        enabled: true
      profile: default
      label: master

server:
  port: 8765
